<Page x:Class="PHILOBM.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Paramètres">
    <Grid Margin="10">
        <!--<TextBlock Text="Paramètres" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        <StackPanel>
            <CheckBox Name="ShowMessageBoxCheckBox" 
                      Content="Afficher les MessageBox" 
                      IsChecked="{Binding ShowMessageBoxes, Mode=TwoWay}" />
            <Button Content="Enregistrer" Click="SaveButton_Click" Margin="0,10,0,0"/>
        </StackPanel>-->
    </Grid>
</Page>
